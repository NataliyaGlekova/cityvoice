import React from "react";
import { Container } from "react-bootstrap";
import MainList from "../../features/Main/ui/MainList";

export default function MainPage(): React.JSX.Element {
  return (
    <Container>
      <MainList />
    </Container>
  );
}
